#server:
#  port: 1111
##  context-path: /microo
#eureka:
#  instance:
#    hostname: localhost
#  client:
#    register-with-eureka: false
#    fetch-registry: false
#    service-url:
#      default-zone: http://${eureka.instance.hostname}:${server.port}/er
server:
  port: 8761

spring:
  application:
    name: EUREKA

eureka:
  instance:
    hostname: peer1
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
    register-with-eureka: false
    fetch-registry: false
  server:
    eviction-interval-timer-in-ms: 1000
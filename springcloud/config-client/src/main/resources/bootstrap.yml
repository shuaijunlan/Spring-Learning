spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:7001/
      profile: dev
      label: master

server:
  port: 2001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1111/eureka/
management:
  security:
    enabled: false
